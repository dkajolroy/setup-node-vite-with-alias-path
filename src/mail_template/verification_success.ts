// email or phone verify dynamic html
export default function verificationSuccess(name: string, caption: string) {
  // verification success mail
  // caption = your phone|email is verified
  return name;
}
